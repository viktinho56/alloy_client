import CustomLayout from "../layouts/CustomLayout";
import BookViewingFeature from "../features/book-viewing/BookViewingFeature";

const BookViewingPage = () => {
  return (
    <CustomLayout>
      <BookViewingFeature />
    </CustomLayout>
  );
};

export default BookViewingPage;
