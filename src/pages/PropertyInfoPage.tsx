import PropertyInfo from "../features/dashboard/PropertyInfo";
import AdminDashboardLayout from "../layouts/AdminDashboardLayout";

const PropertyInfoPage = () => {
  return (
    <AdminDashboardLayout>
      <PropertyInfo />
    </AdminDashboardLayout>
  );
};

export default PropertyInfoPage;
